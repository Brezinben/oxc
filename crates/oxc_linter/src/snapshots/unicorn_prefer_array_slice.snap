---
source: crates/oxc_linter/src/tester.rs
---

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:14]
 1 │ process.argv.splice(2)[0]
   ·              ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(index)[0]
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(index)[offset]
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(index).at(0)
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:14]
 1 │ object.array.splice(index)[0]
   ·              ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(/* comment */ index)[0]
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:25]
 1 │ const array = []; array.splice(index)[0]
   ·                         ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:87]
 1 │ const BaseArray = Array; class ArraySubclass extends BaseArray {} new ArraySubclass().splice(0)[0]
   ·                                                                                       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:67]
 1 │ const ArraySubclass = class extends Array {}; new ArraySubclass().splice(0)[0]
   ·                                                                   ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:58]
 1 │ class ArraySubclass extends Array {} new ArraySubclass().splice(0)[0]
   ·                                                          ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:79]
 1 │ class ArraySubclass extends Array {} const array = new ArraySubclass(); array.splice(0)[0]
   ·                                                                               ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:60]
 1 │ class ArraySubclass extends Array { method() { return this.splice(0)[0]; } }
   ·                                                            ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(index as number)[0]
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(<number>index)[0]
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(index!)[0]
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:7]
 1 │ array.splice(index satisfies number)[0]
   ·       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:21]
 1 │ (array as string[]).splice(index)[0]
   ·                     ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:38]
 1 │ declare const array: string[]; array.splice(index)[0]
   ·                                      ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:62]
 1 │ type Strings = string[]; declare const array: Strings; array.splice(index)[0]
   ·                                                              ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:37]
 1 │ declare const value: unknown; value.splice(index)[0]
   ·                                     ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:55]
 1 │ type Value = Value; declare const value: Value; value.splice(index)[0]
   ·                                                       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:28]
 1 │ const array = array; array.splice(index)[0]
   ·                            ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:47]
 1 │ function method(this: string[]) { return this.splice(0)[0]; }
   ·                                               ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:88]
 1 │ class ArraySubclass extends Array<number> {} declare const array: ArraySubclass; array.splice(0)[0]
   ·                                                                                        ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:109]
 1 │ const BaseArray = Array; class ArraySubclass extends BaseArray {} declare const array: ArraySubclass; array.splice(0)[0]
   ·                                                                                                             ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:87]
 1 │ class ArraySubclass extends Array<number> {} const array = new ArraySubclass(); array.splice(0)[0]
   ·                                                                                       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:78]
 1 │ function getArray(): string[] { return []; } const array = getArray(); array.splice(0)[0]
   ·                                                                              ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:12]
 1 │ for (array.splice(0)[0] of values);
   ·            ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:13]
 1 │ (x as any)!.splice(0)[0]
   ·             ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:23]
 1 │ (x satisfies unknown).splice(0)[0]
   ·                       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:39]
 1 │ declare const x: readonly string[]; x.splice(0)[0]
   ·                                       ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:30]
 1 │ declare const x: [string]; x.splice(0)[0]
   ·                              ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:51]
 1 │ function f<T>(): Set<T> { return new Set(); } f().splice(0)[0]
   ·                                                   ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.

  ⚠ unicorn(prefer-array-slice): Prefer `Array#slice()` over `Array#splice()` when reading from the returned array.
   ╭─[prefer_array_slice.ts:1:277]
 1 │ declare const a0: unknown; const a1 = c ? a0 : a0; const a2 = c ? a1 : a1; const a3 = c ? a2 : a2; const a4 = c ? a3 : a3; const a5 = c ? a4 : a4; const a6 = c ? a5 : a5; const a7 = c ? a6 : a6; const a8 = c ? a7 : a7; const set = new Set(); const value = c ? a8 : set; value.splice(0)[0]
   ·                                                                                                                                                                                                                                                                                     ──────
   ╰────
  help: `Array#splice()` removes the elements from the source array. Use `Array#slice()` to read the elements without changing the source array.
